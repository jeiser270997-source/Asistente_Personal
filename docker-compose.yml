version: '3.8'

services:
  lifeos-dashboard:
    build: 
      context: ./dashboard
      dockerfile: Dockerfile
    container_name: lifeos-dashboard
    ports:
      - "3000:3000"
    environment:
      - IS_DOCKER=true
    volumes:
      # Montamos las carpetas de datos del host como solo-lectura para seguridad
      - ./data:/app/data:ro
      - ./runtime:/app/runtime:ro
      - ./scripts:/app/scripts:ro
    restart: unless-stopped
